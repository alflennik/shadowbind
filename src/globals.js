let components = []
let events = {}
let previousState = null
let repeaterCount = 0
let repeaters = {}
let trace
let bindMethodUsed

export {
  components,
  events,
  previousState,
  repeaterCount,
  repeaters,
  trace,
  bindMethodUsed
}
